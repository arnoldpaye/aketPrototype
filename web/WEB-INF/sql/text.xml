<?xml version="1.0" encoding="UTF-8"?>
<statements>
    <st id ="getAllTexts">
        SELECT 
        txt_id, txt_code, txt_title, txt_author, txt_text,
        txt_keywords_mac, txt_keywords_textrank
        FROM text;
    </st>
    <st id="insertText">
        INSERT INTO text(txt_code, txt_title, txt_author, txt_text, txt_keywords_mac)
        VALUES (#{txtCode}, #{txtTitle}, #{txtAuthor}, #{txtText}, #{txtKeywordsMac});
    </st>
    <st id="editText">
        UPDATE text
        SET
        txt_code = #{txtCode}, txt_title=#{txtTitle}, txt_author=#{txtAuthor}, 
        txt_text=#{txtText}, txt_keywords_mac = #{txtKeywordsMac}
        WHERE txt_id = #{txtId};
    </st>
    <st id="deleteText">
        DELETE FROM text
        WHERE txt_id = #{txtId};
    </st>
    <st id="updateKeyWords">
        UPDATE text
        SET txt_keywords_textrank = #{txtKeywordsTextRank}
        WHERE txt_id=#{txtId};
    </st>
</statements>