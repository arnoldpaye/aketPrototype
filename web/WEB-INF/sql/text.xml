<?xml version="1.0" encoding="UTF-8"?>
<statements>
    <st id ="getAllTexts">
        SELECT txt_id, txt_title, txt_author, txt_text
        FROM text;
    </st>
    <st id="insertText">
        INSERT INTO text(txt_title, txt_author, txt_text)
        VALUES (#{txtTitle}, #{txtAuthor}, #{txtText});
    </st>
    <st id="editText">
        UPDATE text
        SET txt_title=#{txtTitle}, txt_author=#{txtAuthor}, txt_text=#{txtText}
        WHERE txt_id = #{txtId};
    </st>
</statements>