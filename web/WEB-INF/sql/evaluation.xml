<?xml version="1.0" encoding="UTF-8"?>
<statements>
    <st id="getAllEvaluations">
        SELECT 
        e.ev_id, t.txt_title, e.ev_precision, e.ev_recall, e.ev_fmeasure
        FROM evaluation e INNER JOIN text t ON e.ev_txt_id = t.txt_id;
    </st>
    <st id="insertEvaluation">
        INSERT INTO evaluation(ev_txt_id, ev_precision, ev_recall, ev_fmeasure)
         VALUES (#{evTxtId}, #{evRecall}, #{evPrecision}, #{evFMeasure});
    </st>
</statements>